---
phase: 05-irrevocability-and-verdict-semantics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - detecter/src/regeneration/agm_engine.erl
  - detecter/src/synthesis/maxhml_agm_codegen.erl
  - detecter/src/synthesis/maxhml_eval.erl
  - detecter/test/regeneration/agm_engine_test.erl
  - detecter/test/regeneration/generated_agm_recovery_test.erl
autonomous: true
requirements:
  - VERD-01
  - VERD-02
must_haves:
  truths:
    - "After deterministic recovery, the consumed lookahead is processed once through the same generated branch semantics as an ordinary trace event (D-01, D-02, D-03, D-04)."
    - "A replay envelope that is irrelevant in the recovered state remains pending across later state transitions and is reconsidered before newer mailbox envelopes, preserving selective-receive ordering (D-02, D-04)."
    - "Continuation identity remains unchanged while generated bridges map recursive and max-node continuations to their actual receive-state replay entry."
    - "Recovery commits previous_state and current_state together only after one monitoring consequence has been established (D-11)."
    - "Every withholding result preserves the pre-recovery ETS state and retains the established reason atoms (D-09, D-10, D-12, D-13)."
  artifacts:
    - path: detecter/src/synthesis/maxhml_agm_codegen.erl
      provides: "Envelope-aware callbacks, retention-preserving pending dispatch, recursive/max replay bridges, atomic recovery commit, and test-only checkpoint generation"
    - path: detecter/src/synthesis/maxhml_eval.erl
      provides: "Ordinary receive clauses plus pending-envelope receive variants and bridge metadata generated from the same branch bodies"
    - path: detecter/test/regeneration/generated_agm_recovery_test.erl
      provides: "Generated-runtime evidence for exact replay, retention across state transitions, recursive/max bridge routing, single state advancement, no speculative commit, gated queueing, and production checkpoint isolation"
  key_links:
    - from: detecter/src/synthesis/maxhml_eval.erl
      to: detecter/src/synthesis/maxhml_agm_codegen.erl
      via: "Each generated receive state supplies ordinary clauses, pending-propagating branch bodies, continuation identity, and resolved receive-state target to the AGM replay generator"
      pattern: "generate_.*dispatch|generate_missing_event_recovery_case"
    - from: "unchanged {continue, Function, CompleteArgs} consequence identity"
      to: "actual generated receive-state pending dispatcher"
      via: "A generated replay bridge follows max/recursive internal transitions and carries the original envelope plus complete bound arguments"
      pattern: "replay_.*|pending_.*"
    - from: detecter/src/synthesis/maxhml_agm_codegen.erl
      to: detecter/src/regeneration/agm_engine.erl
      via: "Generated orchestration resolves recovery and consequence before invoking an envelope-aware continuation"
      pattern: "resolve_monitoring_consequence|recover_missing_event"
    - from: detecter/src/synthesis/maxhml_agm_codegen.erl
      to: sus_state
      via: "One list-valued ets:insert commits both recovery-state tuples after agreement"
      pattern: "ets.*insert"
---

<objective>
Make generated missing-event recovery preserve and directly replay the original lookahead envelope, with one post-agreement atomic state commit.

Purpose: Close the orchestration defect that can skip the consumed lookahead or expose speculative recovery state, while keeping `agm_engine` pure and preserving the existing verdict domain (D-01 through D-04, D-09 through D-13).
Output: Envelope-aware generated continuations, shared replay dispatchers, a read-only `handle_missing_event/1`, an atomic `commit_recovery_state/1`, deterministic test checkpoints, and focused regression evidence.
</objective>

<execution_context>
@/Users/andretheuma/.codex/gsd-core/workflows/execute-plan.md
@/Users/andretheuma/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-irrevocability-and-verdict-semantics/05-CONTEXT.md
@.planning/phases/05-irrevocability-and-verdict-semantics/05-RESEARCH.md
@.planning/phases/05-irrevocability-and-verdict-semantics/05-VALIDATION.md
@.planning/phases/04.1-agm-engine-extraction-and-thesis-reconciliation/04.1-02-SUMMARY.md
@.planning/phases/04.1-agm-engine-extraction-and-thesis-reconciliation/04.1-VERIFICATION.md
</context>

## Artifacts this phase produces

- Updated `agm_engine` reduction/continuation types accepting one opaque lookahead envelope; no engine effect or invocation is added.
- Generated private `commit_recovery_state/1`.
- Generated private replay bridges and pending-envelope dispatchers named deterministically from each consequence identity and actual receive-state function.
- A generated reference-tagged checkpoint receive clause present only when the generator is compiled with `TEST`.
- A gated-worker test helper that receives a tagged start message before entering generated code, proving earlier same-sender envelopes are already queued.
- Focused EUnit cases in the existing engine and generated-monitor suites; this plan creates no new source module.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the pure consequence callback contract envelope-aware</name>
  <files>detecter/src/regeneration/agm_engine.erl, detecter/test/regeneration/agm_engine_test.erl</files>
  <read_first>
    - `detecter/src/regeneration/agm_engine.erl` — `reduction()` and `resolve_monitoring_consequence/2`.
    - `detecter/test/regeneration/agm_engine_test.erl` — equal, conflicting, exact, and symbolic consequence cases.
    - `.planning/phases/04.1-agm-engine-extraction-and-thesis-reconciliation/04.1-CONTEXT.md` — pure-engine ownership decision.
  </read_first>
  <behavior>
    - A successful consequence still compares only `{verdict, yes|no}` or `{continue, Function, CompleteArgs}`.
    - The returned continuation accepts one opaque envelope but is never invoked by `agm_engine`.
    - Equal signatures still deduplicate even when callback function identities differ.
    - All four established withholding atoms remain unchanged.
  </behavior>
  <action>
Update the `reduction()` type and successful result type in `agm_engine.erl` so the continuation is a unary function accepting the preserved lookahead envelope. Do not add state-table access, mailbox handling, message dispatch, replay, verdict, or callback invocation to the engine; D-01 assigns those effects to the generated monitor and the Phase 4.1 purity boundary remains locked. Update `agm_engine_test.erl` callbacks and assertions to use unary functions and add a guard test proving the engine returns the callback without invoking it. Preserve consequence signature construction and deduplication exactly, because complete bound arguments remain part of identity.
  </action>
  <verify>
    <automated>
      1. `make -C detecter compile-test`
      2. `cd detecter; erl -noshell -pa ebin -eval 'case eunit:test(agm_engine_test, [verbose]) of error -> init:stop(1); Result -> Result end.' -s init stop`
      3. `test -z "$(rg -n 'ets:|receive|![[:space:]]|acceptance|rejection' detecter/src/regeneration/agm_engine.erl)"`
    </automated>
  </verify>
  <acceptance_criteria>
    - `agm_engine_test` passes with unary callbacks and proves no callback execution occurs inside the engine.
    - `agm_engine.erl` remains effect-free and exports no new orchestration API.
    - Existing signature and reason-atom assertions remain intact.
  </acceptance_criteria>
  <done>The pure engine returns but never invokes a unary envelope callback, with all existing consequence and withholding semantics passing their focused tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate ordered replay, atomic commit, and deterministic checkpoints</name>
  <files>detecter/src/synthesis/maxhml_agm_codegen.erl, detecter/src/synthesis/maxhml_eval.erl, detecter/test/regeneration/generated_agm_recovery_test.erl</files>
  <read_first>
    - `detecter/src/synthesis/maxhml_agm_codegen.erl` — `generate_missing_event_recovery_case/1`, `monitor_reduction_pair/3`, `generate_handle_missing_event_function/0`, state-management helpers.
    - `detecter/src/synthesis/maxhml_eval.erl` — conjunction and necessity `generate_function/2` receive-clause construction.
    - `detecter/test/regeneration/generated_agm_recovery_test.erl` — generated compilation harness, state setup, worker helpers, and current continuation observation.
    - `detecter/src/synthesis/gen_eval.erl` — `pat_tuple/1`, the canonical trace-envelope pattern generator.
  </read_first>
  <behavior>
    - `handle_missing_event/1` returns `{ok, Recovery, OriginalEnvelope}` without changing ETS.
    - Consequence withholding returns `{withhold, Reason}`, terminates, emits no verdict, and leaves both recovery-state entries unchanged.
    - Successful recovery performs one list-valued ETS insert for `previous_state` and `current_state`, then invokes the unary continuation with the untouched envelope.
    - A matching replay executes the ordinary branch body and advances from the inferred state exactly once.
    - An irrelevant replay remains an explicit pending envelope; if a newer mailbox event advances the monitor, the pending envelope is tested first against the new state before any still-newer mailbox event.
    - A consequence whose unchanged identity names a recursive/max function such as `x` enters a generated bridge that follows internal transitions to the actual receive-state pending dispatcher with all bound arguments intact.
    - A reference-tagged checkpoint acknowledges the reached generated state only in test-generated monitors.
    - A production-compiled generator takes an explicit no-op conditional branch and emits no checkpoint clause or control atom.
  </behavior>
  <action>
Refactor the generated effect boundary per D-01 through D-04 and D-11. Bind the complete concrete `send` trace tuple as `LookaheadEnvelope` in `generate_handle_missing_event_function/0`; use its payload only for engine inference and return the untouched tuple with `Recovery`. Move all recovery mutation into a generated `commit_recovery_state/1` called only from the successful consequence branch, and build one `ets:insert/2` AST whose object list contains both `{previous_state, Source}` and `{current_state, Inferred}`.

Make `monitor_reduction_pair/3` emit unary callbacks without changing the compared consequence signature `{continue, Function, CompleteArgs}`. Verdict callbacks may ignore the later envelope because they terminate. Every continuing callback must call a deterministic private replay bridge for `Function`, not assume that `Function` itself owns a receive. Generate bridge metadata while traversing the formula: an actual receive-state identity maps to its pending dispatcher, while max/recursive identities such as `x` follow the same zero-event internal transition as the ordinary generated function until they reach the actual receive state. The bridge carries the original pending envelope and the complete bound-argument mapping throughout. Add focused generated tests for `prop_no_leak` where the selected identity is recursive `x`, for entry through a max node, and for a recursive case with bound arguments; assert that replay reaches the expected receive-state dispatcher without altering the consequence identity.

Preserve true selective-receive behavior with an explicit pending-envelope path generated from the same ordinary branch descriptors. A pending dispatcher first tests the retained envelope against the current state's exact patterns and guards. On a match, execute the ordinary branch body once and consume the pending envelope. On no match, enter a receive containing the ordinary clauses without a catch-all; when a newer mailbox envelope matches and causes a continuing transition, use a pending-propagating branch body that carries the older envelope through the next replay bridge. Reconsider that older envelope before scanning remaining mailbox messages in every reached state. Do not discard an unmatched replay envelope and do not send it through the mailbox. Add a retention-sensitive equivalence fixture where the replay envelope is irrelevant in the recovered state, a newer queued envelope advances the monitor, and the retained older envelope becomes relevant in the next state; the complete and recovered encodings must produce the same observation.

Add a generator helper under `-ifdef(TEST)` that appends `{agm_checkpoint, Ref, TestPid}` to non-terminal receive states and an explicit `-else` implementation returning no clauses. A production-compiled generator must therefore exercise a concrete no-op branch. Add a tagged gated-worker helper that reports ready, selectively waits for `{phase5_start, Ref}`, and only then invokes the generated entry function; tests enqueue trace envelopes and finally send the start tag from the same sender, proving all preceding envelopes were delivered before generated processing begins. Replace scheduler/introspection continuation polling with tagged checkpoints. Keep non-`send` lookahead and additional uncertainty protocols outside this plan.
  </action>
  <verify>
    <automated>
      1. `make -C detecter compile-test`
      2. `cd detecter; erl -noshell -pa ebin -eval 'case eunit:test(generated_agm_recovery_test, [verbose]) of error -> init:stop(1); Result -> Result end.' -s init stop`
      3. `set -e; TmpDir=$(mktemp -d); trap 'rm -rf "$TmpDir"' EXIT; cp detecter/test/props/prop_no_leak.hml "$TmpDir/phase5_production.hml"; cp detecter/priv/sys_info.spec "$TmpDir/phase5_production.spec"; make -C detecter clean compile; cd detecter; erl -noshell -pa ebin -eval "ok = maxhml_eval:compile(\"$TmpDir/phase5_production.hml\", [{outdir, \"$TmpDir\"}, {mtab, \"$TmpDir/phase5_production.spec\"}, erl]), init:stop()."; test -z "$(rg -n 'agm_checkpoint' "$TmpDir"/*.erl)"`
      4. `make -C detecter test`
      5. `test -z "$(rg -n 'timer:sleep|process_info' detecter/test/regeneration/generated_agm_recovery_test.erl)"`
    </automated>
  </verify>
  <acceptance_criteria>
    - The generated helper returns the exact received envelope and does not mutate `sus_state`.
    - Successful consequence agreement commits both state entries through one ETS operation before direct dispatch.
    - Matching replay behaves as the ordinary generated state would; unmatched replay remains pending across a later transition and is reconsidered before newer mailbox messages.
    - Recursive/max consequence identities, including `x` in `prop_no_leak`, route through generated bridges to the actual receive-state pending dispatcher with complete arguments.
    - Withholding on consequence disagreement leaves both state entries unchanged.
    - New claim-critical synchronization uses tagged messages and process monitors, with no scheduler-delay or process-introspection polling.
    - A production-clean compile and representative production generation contain no checkpoint control tag, while test generation still exercises the checkpoint clause.
  </acceptance_criteria>
  <done>Generated recovery preserves and directly dispatches the original envelope after one atomic post-agreement commit, and deterministic generated-runtime tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Relevant risk | Required control |
|---|---|---|
| Trace sender to generated replay dispatcher | A reconstructed, loosely matched, or discarded envelope can alter clause selection, ordering, or state updates. | Preserve the original term in explicit pending state, retain ordinary tuple patterns/guards, and propagate it across transitions until matched or terminal (D-02, D-04). |
| Consequence identity to replay entry | Recursive/max identities may name internal functions without receives. | Preserve identity for engine comparison and route execution through generated bridges to the actual receive-state pending dispatcher. |
| Pure recovery result to public ETS state | Speculative or partially visible recovery state can contaminate later monitoring. | Resolve the complete consequence first, then commit both tuples in one list-valued insert (D-11). |
| EUnit control messages to generated monitor protocol | A checkpoint clause in production would expand the monitor protocol. | Provide an explicit production no-op conditional branch and prove in Wave 1 that production-generated source lacks the control tag; Plan 05-03 repeats the gate. |
</threat_model>

<verification>
- The focused engine suite and generated AGM suite pass.
- The complete `make test` gate passes.
- Generated source demonstrates direct envelope dispatch and a single post-agreement recovery commit.
- Retention-sensitive replay and recursive/max bridge tests pass.
- Production generation excludes the test-only checkpoint in Wave 1.
- `agm_engine` remains pure.
- The claim-critical test file contains no timing or process-state polling.
</verification>

<success_criteria>
- VERD-01 has a correct replay mechanism on which complete/recovered equivalence can be tested.
- VERD-02 cannot be undermined by speculative ETS mutation.
- D-01 through D-04 and D-09 through D-13 are implemented without introducing a new verdict, replay mailbox, or uncertainty method.
</success_criteria>

<output>
Create `.planning/phases/05-irrevocability-and-verdict-semantics/05-01-SUMMARY.md` when complete.
</output>
