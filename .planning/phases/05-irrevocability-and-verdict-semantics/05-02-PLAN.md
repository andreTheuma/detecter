---
phase: 05-irrevocability-and-verdict-semantics
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - detecter/test/regeneration/generated_agm_recovery_test.erl
autonomous: true
requirements:
  - VERD-01
  - VERD-02
must_haves:
  truths:
    - "Complete and deterministically recovered encodings of the same logical trace have the same observable terminal or continuing behavior (D-05, D-06)."
    - "The equivalence matrix covers yes, no, continued no-verdict, exact recovery, and multiple candidates with one consequence (D-08)."
    - "No-verdict evidence is causally synchronized and never inferred from elapsed time (D-07)."
    - "All four ambiguity classes terminate normally with their stable internal reason, no verdict, and unchanged recovery state (D-09 through D-13)."
  artifacts:
    - path: detecter/test/regeneration/generated_agm_recovery_test.erl
      provides: "Complete/recovered equivalence matrix and complete withholding lifecycle matrix"
  key_links:
    - from: detecter/test/regeneration/generated_agm_recovery_test.erl
      to: "generated monitor ordinary and missing-event entry paths"
      via: "The same fixture runs as a complete logical trace and as one explicit missing marker plus lookahead"
      pattern: "complete|recovered|missing_event"
    - from: detecter/test/regeneration/generated_agm_recovery_test.erl
      to: "generated test-only checkpoint"
      via: "Same-sender trace/checkpoint ordering proves equivalent continuation state"
      pattern: "agm_checkpoint"
    - from: "retained replay envelope"
      to: "later generated receive state"
      via: "A gated retention fixture advances on a newer queued event, then proves the older pending envelope is reconsidered in the new state"
      pattern: "phase5_start|pending|retention"
---

<objective>
Prove complete/recovered observable equivalence and conservative withholding over the bounded thesis-representative matrix.

Purpose: Supply deterministic end-to-end evidence for VERD-01 and VERD-02 after ordered replay and atomic recovery commit exist (D-05 through D-13).
Output: Symmetric lifecycle helpers, complete-versus-recovered `yes`/`no`/continuation cases, and strengthened four-reason withholding checks.
</objective>

<execution_context>
@/Users/andretheuma/.codex/gsd-core/workflows/execute-plan.md
@/Users/andretheuma/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-irrevocability-and-verdict-semantics/05-CONTEXT.md
@.planning/phases/05-irrevocability-and-verdict-semantics/05-RESEARCH.md
@.planning/phases/05-irrevocability-and-verdict-semantics/05-VALIDATION.md
@.planning/phases/05-irrevocability-and-verdict-semantics/05-01-SUMMARY.md
@detecter/test/regeneration/generated_agm_recovery_test.erl
</context>

## Artifacts this phase produces

- Reusable generated-test helpers for monitored workers, exact verdict collection, checkpoint acknowledgement, and complete/recovered execution.
- A bounded equivalence matrix covering terminal `yes`, terminal `no`, and equivalent continuing prefixes.
- A withholding lifecycle matrix for `ambiguous_state`, `impossible_recovery`, `ambiguous_consequence`, and `unproven_consequence`.
- No new production file or protocol.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add complete-versus-recovered observable equivalence cases</name>
  <files>detecter/test/regeneration/generated_agm_recovery_test.erl</files>
  <read_first>
    - `detecter/test/regeneration/generated_agm_recovery_test.erl` — suite setup, inline properties/system information, worker lifecycle, and Plan 05-01 checkpoint helper.
    - `detecter/test/props/prop_no_leak.hml` — supported necessity/conjunction/recursion fragment.
    - `detecter/src/synthesis/maxhml_eval.erl` — generated acceptance/rejection and receive-state argument conventions.
    - `.planning/phases/05-irrevocability-and-verdict-semantics/05-RESEARCH.md` — deterministic validation matrix.
  </read_first>
  <behavior>
    - Exact-recovery complete and recovered traces each produce one `yes` and terminate normally.
    - Exact-recovery complete and recovered traces each produce one `no` and terminate normally.
    - Multiple candidate missing events with one complete consequence produce the same terminal outcome as the corresponding complete trace while event metadata remains `unknown`.
    - An irrelevant lookahead leaves complete and recovered executions in equivalent non-verdict continuations; a common relevant suffix then produces the same observation.
    - An initially irrelevant recovered lookahead that becomes relevant after a newer event advances state is retained and observed in the same order as ordinary selective receive.
    - Recursive/max continuation identities reach the same actual receiving state on complete and recovered paths.
    - Corresponding prefixes never diverge through a premature verdict.
  </behavior>
  <action>
Extend the existing generated-module setup with inline acceptance, rejection, continued-monitoring, duplicate-event, and retention-sensitive fixtures within the supported generated fragment. Build one reusable runner that encodes the same logical sequence in two ways: all concrete envelopes for the complete path, and one `{missing_event, From}` followed by the preserved concrete lookahead for the recovered path. Compare only observable behavior per D-05: exact verdict sequence after normal termination, or the reference-tagged continuation checkpoint plus empty verdict collection before a common suffix.

Add the bounded cases required by D-06 and D-08: exact `yes`, exact `no`, multiple literals with one consequence and `event => unknown`, and continued no-verdict after irrelevant lookahead. Add the retention-sensitive row required by D-02/D-04: start-gate both workers, enqueue the full logical sequence before release, make the recovered lookahead irrelevant in the first recovered receive state, use a newer envelope to move both executions to a state where the older envelope is relevant, and assert equal final observation. Exercise the recursive `x` and max-node bridge fixtures from Plan 05-01 so the recovered callback reaches the same actual receiving state as the complete path without changing `{continue, Function, CompleteArgs}`. Use process monitors, worker result messages, the tagged start gate, and tagged checkpoints as D-07 synchronization evidence.
  </action>
  <verify>
    <automated>
      1. `make -C detecter compile-test`
      2. `cd detecter; erl -noshell -pa ebin -eval 'case eunit:test(generated_agm_recovery_test, [verbose]) of error -> init:stop(1); Result -> Result end.' -s init stop`
      3. `cd detecter; for Run in 1 2 3; do erl -noshell -pa ebin -eval 'case eunit:test(generated_agm_recovery_test) of error -> init:stop(1); Result -> Result end.' -s init stop || exit 1; done`
    </automated>
  </verify>
  <acceptance_criteria>
    - Every D-08 equivalence row is represented by a named EUnit case.
    - Complete and recovered executions use identical logical event values and original trace-envelope shapes.
    - Terminal comparisons assert exact verdict lists, not merely receipt of one expected atom.
    - Continuing comparisons prove a tagged checkpoint and no verdict before applying a common suffix.
    - The retention-sensitive case fails if unmatched direct replay is discarded and passes only when the older envelope survives the intervening state transition.
    - Recursive/max bridge rows assert the unchanged continuation identity and the reached receive-state checkpoint.
    - Three consecutive focused-suite runs pass deterministically.
  </acceptance_criteria>
  <done>The bounded complete/recovered `yes`, `no`, multi-event, and continuing-prefix matrix passes through causally synchronized observations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the withholding lifecycle matrix</name>
  <files>detecter/test/regeneration/generated_agm_recovery_test.erl</files>
  <read_first>
    - `detecter/test/regeneration/generated_agm_recovery_test.erl` — existing ambiguity cases and lifecycle helpers after Task 1.
    - `detecter/src/regeneration/agm_engine.erl` — stable reason atoms and recovery/consequence return contracts.
    - `detecter/src/synthesis/maxhml_agm_codegen.erl` — successful commit and withholding branches delivered by Plan 05-01.
  </read_first>
  <behavior>
    - Ambiguous state returns `{withhold, ambiguous_state}`.
    - Impossible recovery returns `{withhold, impossible_recovery}`.
    - Conflicting complete consequences return `{withhold, ambiguous_consequence}`.
    - Unsupported symbolic proof returns `{withhold, unproven_consequence}`.
    - Every case preserves both ETS entries, emits no `yes` or `no`, reports the internal result, and exits with reason `normal`.
    - Additional queued events are not consumed to seek later disambiguation.
  </behavior>
  <action>
Refactor the four existing withholding scenarios to use the monitored-worker lifecycle and exact verdict collector. For each reason required by D-12, snapshot both `previous_state` and `current_state`, start a gated worker and wait for its ready acknowledgement, enqueue the explicit missing marker, lookahead, and an additional concrete envelope, then send the matching `{phase5_start, Ref}` tag from the same test process. Because the gate receives the later start signal selectively, same-sender ordering proves every earlier envelope was already queued before generated recovery begins. Await the worker result and `DOWN` reason `normal`, then assert the snapshot is unchanged and the collected verdict list is empty. This deterministically proves withholding does not consume queued extensions to seek disambiguation under D-09/D-13. Keep `{withhold, Reason}` observable only through the white-box worker result as required by D-10; do not add an external verdict or production protocol.
  </action>
  <verify>
    <automated>
      1. `make -C detecter compile-test`
      2. `cd detecter; erl -noshell -pa ebin -eval 'case eunit:test(generated_agm_recovery_test, [verbose]) of error -> init:stop(1); Result -> Result end.' -s init stop`
      3. `make -C detecter test`
      4. `test -z "$(rg -n 'timer:sleep|process_info' detecter/test/regeneration/generated_agm_recovery_test.erl)"`
    </automated>
  </verify>
  <acceptance_criteria>
    - All four stable reason atoms have explicit lifecycle assertions.
    - Every withholding worker exits normally with no terminal verdict.
    - Both recovery-state entries remain byte-for-byte equal to their pre-recovery values.
    - Withholding does not resume monitoring after a later queued event.
    - The tagged start gate proves the missing marker, lookahead, and extension were queued before generated processing began.
    - The full default regression gate passes without timing or process-state polling in the claim-critical suite.
  </acceptance_criteria>
  <done>All four withholding reasons preserve state, emit no verdict, return their internal reason, terminate normally, and do not continue after uncertainty.</done>
</task>

</tasks>

<threat_model>
## Relevant controls

| Risk | Mitigation evidence |
|---|---|
| A malformed or reconstructed envelope makes complete/recovered comparisons non-corresponding. | Every runner uses the existing concrete trace tuple and changes only the explicit missing marker encoding (D-02, D-05). |
| Test-only checkpoint messages affect production semantics. | The tests consume only the Plan 05-01 `TEST` clause; Plan 05-03 separately generates with production code and checks that the control tag is absent. |
| Withholding leaves globally visible speculative state. | Every reason snapshots and compares both ETS entries after normal worker termination (D-11). |
</threat_model>

<verification>
- The generated AGM suite passes three consecutive focused executions.
- The complete default suite passes.
- VERD-01 rows cover `yes`, `no`, and a continuing prefix for exact and multi-event deterministic recovery.
- VERD-01 includes retention-sensitive and recursive/max bridge rows that would fail if replay were discarded or routed to a non-receiving identity.
- VERD-02 rows cover all four stable withholding reasons, unchanged state, no verdict, and normal termination.
- The claim-critical suite contains no scheduler-delay or process-introspection polling.
</verification>

<success_criteria>
- VERD-01 is demonstrated by paired complete/recovered observations rather than internal state identity.
- VERD-02 is demonstrated for every supported recovery/consequence ambiguity class.
- D-05 through D-13 are covered without broadening the verdict domain or continuing after uncertainty.
</success_criteria>

<output>
Create `.planning/phases/05-irrevocability-and-verdict-semantics/05-02-SUMMARY.md` when complete.
</output>
