with
    calc_server:loop(_)
check
    [{_ <- _, calc_server:loop(_)}]
    max X.(
        [{_ ? {_, {add, A, B}}}](
            [{_:_ ! {ok, Res} when Res =:= A + B}]ff
            and
            [{_:_ ! {ok, Res} when Res =/= A + B}]X
        )
    ).